db:
  username: test
  password: test

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=tables1
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    default_schema: tables1
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        hbm2ddl.auto: validate
        default_schema: tables1
